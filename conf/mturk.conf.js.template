module.exports =  {

    // AWS secret and access.

    access : 'YOUR_ACCESS_CODE',
    secret : 'YOUR_SECRET',

    // Options below can be overwritten by command-line parameters.

    sandbox: true,

    // The name of the bonus field in the results file (default: "bonus").
    bonusField: 'bonus',

    // The name of the ExitCode field in the results file (default: "ExitCode").
    // Notice: in the results file obtained from AMT, it is named after the id
    // of the HTML input element in the MTurk frame.
    exitCodeField: 'Answer.surveycode',

    HITId: 'XXX',

    // Id of the qualification to assign upon approving/rejecting
    // an assignment. Default: undefined.
    QualificationTypeId: 'YYY',

    // Turker is notified when a qualification is granted.
    SendNotification: false,

    // Unique token to send for sensitive operations to AMT server.
    // Default: 3000.
    UniqueRequestToken: 1234,

    // Dry-run (no request sent to server).
    dry: false,

    // Path to a results file. Default: undefined.
    resultsFile: 'path/to/results/file',

    // Path to a input codes file. Default: undefined.
    inputCodesFile: 'path/to/input/codes/file',
    
    // Validation level for results.
    validateLevel: 2,

    // Options for loading input and codes file.
    // For full list of available options see https://github.com/nodeGame/NDDB
    loadOptions: {

        // How items are separated in the csv file. Default: ','.
        separator: ';',

        // How strings are quoted in the csv file. Default: '"'.
        quote: '"'

    }

};
